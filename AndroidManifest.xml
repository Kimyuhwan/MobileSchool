<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.MobileSchool"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="10" android:targetSdkVersion="16"/>

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher" android:name=".Utils.GlobalApplication">
        <activity android:name=".Activities.IntroActivity"
                  android:theme="@style/Theme.Openschool" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".SchoolActivity" android:theme="@style/Theme.Openschool"/>
        <activity android:name=".Activities.LogInActivity" android:theme="@style/Theme.Openschool"/>
        <activity android:name=".Activities.EntryActivity" android:taskAffinity="com.android.phone"/>

        <activity android:name=".Activities.DialogBox"
                  android:label="DialogBox"
                  android:theme="@android:style/Theme.NoTitleBar.Fullscreen"/>

        <service android:name=".BroadCastReceiver.ManagerRegistrationService"/>
        <service android:name="com.parse.PushService" />


        <receiver android:name=".BroadCastReceiver.BootCompleteBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <receiver android:name="com.parse.ParseBroadcastReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_PRESENT" />
            </intent-filter>
        </receiver>
        <receiver android:name=".BroadCastReceiver.NotificationClickBroadcastReceiver"/>
        <receiver android:name=".BroadCastReceiver.TelephonyBroadcastReceiver"/>
        <receiver android:name=".Communication.PushReceiver"/>

    </application>
</manifest>
